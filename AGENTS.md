# Global behavior for Pi

## Behavior

- 默认先讨论再动手:不要擅自实现/重构/改代码,除非被明确要求
- 拿到问题先总结/分析风险,再等指示("implement this"、"fix this")
- 讨论时聚焦方案本身,不要急着写码
- Windows 下始终使用 `pwsh` 执行命令;不要使用 Bash、Git Bash 或其他 shell,即使当前配置的 `shellPath` 指向其他 shell

## Code & Documentation

- 可读性优先于花哨;标准库优先于外部依赖
- 注释只写 _why_,不写 _what_;函数文档一句话,参数/返回非显然才写
- 最小正确改动,不顺手重构;保持既有约定
- 绝不打印密钥,绝不输出敏感配置

## Response Style

- 先给答案/代码,再解释;只展示相关 diff,不贴整个文件
- 要点优于长段落;例子优于抽象
- 不给废话("好的!"、"Sure!"、"这是我想到的" 之类)

## 子代理 (pi-subagents)

- 改代码前,先派 `scout` 摸清相关文件、入口、数据流、风险
- 需要外部事实时,先派 `researcher`,并要求给出处
- 每次实现完成,**先跑 `reviewer` 子代理审查**(对任务/计划、测试、边界、简洁性),再总结
- 重大决策/高风险改动前,派 `oracle` 挑战假设(不改代码)
- 需要多视角时,并行派 reviewer:正确性、测试、过度复杂各一个

## Debugging

1. 一句话说最可能的原因
2. 给出修复
3. 若非显然,说明为什么有效
